# Press Box backend closure progress

Plan: `docs/plans/2026-08-25-press-box-backend-closure.md`

- Phase 0.2: baseline focused gates passed in the existing dirty worktree (`swift build`; `--screen-read-models`: 74 tests/10,001 checks; `--core-contracts`: 251 tests/3,546 checks; `--jersey-numbers`: 6 tests/925 checks).
- Phase 0.2 review: `depthChart(from:)` is GitNexus HIGH risk (2 direct dependents; load/new-career flows); `proManagement(from:)` is LOW. Changes are additive and covered by the focused suite.
- Phase 0.2 rewrite tournament: skipped because each production hunk is under 15 lines and adds no branching.
- Phase 0.2 confidence review: no confirmed defect; jersey assignment totality, deterministic ordering, released-player nil number, shared-store compilation, and save/load-facing contracts verified by focused tests.
- Pending: full no-argument `SimTests` integration gate.
- Phase 0.3: complete in working tree (practice budget and promotion destination; TDD red/green; independent task review approved with no findings).
- Phase 1: complete in working tree. Root schema 14 preserves schemas 11–13; four-area college ownership migrates once to six areas; current missing/unknown responsibility metadata fails closed; envelope/document remain v1.
- Phase 1 TDD: `.practicePlan` compile RED; schema-14 markerless-current corruption RED; both GREEN after the minimal model/root migration.
- Phase 1 gates: `--save-document` 25 tests/77 checks; `--career-control` 14/105; `--weekly-authority` 5/17; `--portal-contracts` 28/138; `--college-state` 39/4,082; `--e2e-journey` 1/2; `--core-contracts` 251/3,547; all passed. `git diff --check` passed.
- Phase 1 review: spec PASS. Quality review found one markerless schema-14 false-legacy path; fixed at the root decoder and re-review marked RESOLVED. `GameState` conservative GitNexus impact is CRITICAL (165 symbols; restore/new-career/draft-probe flows), so broad restore and contract gates were retained.
- Phase 1 rewrite tournaments: count-based exact legacy ownership decoder won and passed 25/77 save checks; direct invalid-state root probe won and passed 25/77 plus E2E/core gates.
- Phase 1 confidence review: marker emission, missing/current/future marker errors, typed-key completeness, schema range, stable second round-trip, and no-college paths verified. No remaining Phase 1 defect.
- Phase 2: complete in the working tree. College and professional authority each expose six exact responsibilities, named delegates share a two-area capacity bound, every advertised delegated area has an existing-system consumer, cruise is persisted/resumable/bounded, and Responsibilities/While Away projections read authoritative state.
- Phase 2 TDD/confidence fixes: split recruiting/NIL authority now preserves user-owned NIL, scouting, and relationship evidence; automatic redshirt decisions append an atomic deterministic `decisionDelegated` event; retained delegated-activity authors survive staff pruning; activity history evicts at 512 entries; cruise refuses >52 weeks and resumes after save/reload without duplicate activities.
- Phase 2 rewrite tournaments: the partial recruiting/NIL isolation candidate won and passed `--career-control`; the explicit cruise loop incumbent held; the inline automatic-redshirt transaction incumbent held after confidence fixes.
- Phase 2 gates: `--career-control` 15 tests/130 checks; `--weekly-authority` 8/62; `--professional-career-session` 3/21; `--event-ledger-batch` 12/56; `--staff-pruning` 1/9; all passed. `git diff --check` passed.
- Phase 2 risk: `CareerMandatoryDecisionSystem.refresh` is HIGH (3 direct callers, 109 affected symbols); retained identity pruning is CRITICAL (2 direct callers, 46 affected symbols, 2 processes/6 modules). Focused annual, scheduler, event, and pruning gates cover both shared paths.
- Phase 3: complete in the working tree. FootballSimCore now exposes exact release, extension, practice-squad promotion, waiver, and unavailable-restructure projections; the controlled pro team receives one persisted cap-compliance checkpoint, and direct/intent/cruise advancement cannot cross it.
- Phase 3 TDD/confidence fixes: multi-release and progressive-extension sequences survive save/reload without replay; releases must stay legal or strictly reduce an existing overage; checked arithmetic prevents projection overflow; cross-team projections cannot leak another team's checkpoint; a real year-0/year-1 contract escalation creates the checkpoint at rollover.
- Phase 3 boundary fix: postseason portal commit validates against a next-season integrity projection, so it now refreshes the controlled cap checkpoint in that projection before checking the whole root. This closed a real `portalCommitFailed(.postseason)` found by the boundary confidence test.
- Phase 3 rewrite tournament: the legal-action projection incumbent held; negotiation settlement dropped one redundant second roster-wide cap scan while retaining its public deadline guard. No wider rewrite won.
- Phase 3 gates: `--pro-management` 10 tests/32 checks; `--pro-market` 17/125; `--professional-career-session` 3/21; `--cap-compliance` 14/80; `--portal-transaction` 17/133; all passed. `git diff --check` passed before the final confidence tranche and will be repeated at integration.
- Phase 3 risk: `checkProfessionalCap`, `IntentResolver.resolve`, `WorldScheduler.advanceWeek`, and `settleNegotiation` are CRITICAL shared paths; `CareerSession.resolve` is HIGH. Exact-money, direct advance, actor resolution, save/reload, real rollover, portal, and existing pro-system gates cover their changed branches.
- Phase 4: complete in the working tree. Career state signs one immutable preseason expectation per job/season; weekly and season-end support consume it. The existing bounded coach season record now retains the compact Season Review outcome, and the transient current-job title snapshot retains only the exact tier `.championship` result and at most three player-stat evidence rows before rollover.
- Phase 4 confidence fixes: rich outcomes require a tier; professional ranks are capped at 32 and cannot carry a recruiting-class rank; college/pro records use their own maximum game totals; nonempty milestones require a tier; title scores are bounded; and an employed or just-ended job cannot decode another organisation's title result. Legitimate fired-coach title state still round-trips.
- Phase 4 gates: `--career-outcomes` 6 tests/42 checks; optimized `--career-arc` 27/413; optimized `--coach-season-record` 3/31; `--history-archive` 20/147; `--history-read-model` 4/24; `swift build` and `git diff --check` passed. The independent `--season-rollover` lane was stopped after an excessive silent runtime and is not claimed green; its overlapping real career rollover path passed in `--career-arc` and `--coach-season-record`, and the lane remains a Phase 7 performance finding.
- Phase 4 rewrite tournament: incumbent `CareerSeasonOutcomeSystem.snapshot` and `CoachSeasonRecord.isValid` held. Speed/readability validators regressed proven tier bounds; the memory candidate preserved them but replaced bounded once-per-season work with a larger nested scan/bitmask implementation. No tournament rewrite applied.
- Phase 4 confidence review: confirmed and fixed cross-tier record bounds and unrelated-title decoding. Verified exact pre-rollover capture order in `WorldScheduler`, conference/tier-stage separation for both tiers, signed-target immutability, compact evidence/history bounds, legacy basic-record decoding, save/reload projection stability, fired-coach rollover, and pro outcome semantics. No Phase 4 correctness issue remains open; only the non-potent rollover-suite duration remains for Phase 7.
- Phase 5: complete in the working tree. `CompareReadModel` is exactly two existing `RosterReadModel.PlayerRow` projections and `comparePlayers` rejects identical, absent, or off-roster identities without persisting selection or player truth. HQ staff recommendations require a real non-head organisation staff member, the engine-selected recruiting option, recorded reasons, and an exact existing scouting observation; roster summaries require the real matching position coach and cite only known availability, overall, scheme fit, and condition.
- Phase 5 rewrite tournament: the readability candidate won for `staffRecommendation`/`playerStaffAnalysis`; it moved evidence guards ahead of staff work and named the three profile facts without changing signatures, deterministic rating/UUID ties, or truthful nil behavior. The verified winner passed `--screen-read-models`.
- Phase 5 confidence review: pinned equal-rating UUID tie-breaking, missing Compare identity, same-player rejection, live player-row refresh, absent scouting evidence, and absent qualified-staff degradation. Verified no hidden potential, medical diagnosis, scouting range, new persistence, or UI view construction. No Phase 5 correctness issue remains open.
- Phase 5 gates: `--screen-read-models` 76 tests/10,020 checks; `--staff-pruning` 1/9; `swift build`; `git diff --check`; all passed. GitNexus remains indexed against the sibling checkout, so new-symbol impacts were UNKNOWN; conservative parent impacts remain `coachingHQ` CRITICAL (5 direct; load/new-career/inbox/opponent-film) and `profile` HIGH (2 direct; roster/health/inbox/profile). Cross-worktree `detect_changes` was rejected by GitNexus and is not claimed.
- Phase 6: complete in the working tree. Call-in pacing is authoritative, resumable, clamped to 12...40, and keeps urgent situations eligible. Workhorse, Mentor, Adaptable, Ice in Veins, and Front Runner now have bounded consumers before joining the explicit generated-trait allow-list.
- Phase 6 gates: `--engine` 67 tests/32,510 checks; `--match-reducer` 21/101; `--trait-population` 8/635; focused People lifecycle groups for state, staff, health, and development; all passed. `git diff --check` passed for the tranche.
- Phase 6 rewrite/confidence: development context collapsed to one typed per-player record; snap resolution precomputes venue sides. Confidence review fixed future trait cases activating before their mechanics, and added Mentor boundary, defense-side hostile venue, and malformed pacing trust-boundary regressions.
- Phase 7 performance: the potent Release host gate first failed at a 7.668s median (`[7.324, 7.668, 7.771]`). Sampling found `DisciplineSystem.incidents` repeatedly recomputed organisation standings and latest results for every player. A shared per-organisation morale derivation preserves exact public readings and incident RNG while resolving that context once.
- Phase 7 performance gates: `--discipline` 10 tests/197 checks passed; fresh Release `--performance-budget` passed at 1.839s median (`[1.828, 1.839, 1.937]`) against the 2.000s host ceiling. The separate 1.200s supported-device target remains a machine performance check; the 6–8 hour season target is non-gating product direction and requires no human approval.
- 2026-08-26 gate policy: required season-running SimTests are reduced to a 10-season horizon for M1/M2/M3/M7 and E2E-H. Longer 20/30+ season runs remain optional diagnostics; the product's 20-season durability direction is not a release gate.
