# Future Simulation Contract

This is the repository-local register for promises that cannot yet be implemented truthfully.
Entries become active only when their named dependency owns authoritative state. A UI placeholder,
random value, or prose-only event does not satisfy an entry.

| ID | Required future behavior | Missing dependency | Activation milestone |
|---|---|---|---|
| FSC-001 | **Activated in M1.** Validate every hot domain-event entity reference against the authoritative root. | M1 game, postseason, and season-completion payloads carry organisation IDs; `eventReferences` is active. Historical game IDs remain event identities rather than normalized entity references. | Complete for current payloads; extend with each new payload. |
| FSC-002 | **Partially activated in M7B.** An event leaving the bounded hot journal folds into a `SeasonHistoryDigest` for its own season: that season's archived count plus a bounded, ranked sample of bodies, surfacable through `digest(forSeason:)` without reading the journal or the save. Measured over 30 seasons: 2,032,988 archived events reduce to 30 digests and 960 retained bodies. | Bodies are retained for *ranked-notable* events only and only within bounds, so the great majority of archived events keep no body at all. Full cold storage of every archived body, and event-ID uniqueness across hot and cold history, remain unbuilt. | Persistence/storage milestone. |
| FSC-003 | Compress and, if measurements require it, chunk/stream snapshots; reject oversized imports before allocating their complete body. | **Now measured past season 20 rather than extrapolated, and worse than previously recorded.** M7B's 30-season gate measured **42.4 MB at season 1, 70.1 MB at 5, 213.9 MB at 20 and 306.9 MB at 30**, against M2's recorded 84.66 MB at season 20 and an original 8 MB production ceiling. Encoding alone costs 10.16 s at season 30 on a development Mac. `SaveEnvelope` still has header validation but no compression, streaming import, or budget enforcement. | **Release blocker.** Persistence and performance milestone. |
| FSC-004 | **Activated in M1.** Validate schedule references/results, regular-season standings, statistics, bracket fields, prior-round winners, champions, compact archives, and record context. | Current M1 competition state is authoritative and its projections are rebuilt from results. | Complete for M1; extend when later competition formats are added. |
| FSC-005 | **Activated in M3.** Eligibility, exact scholarship ownership, prospect cycles, scouting, recruiting relationships, commitment capacity/conservation, recorded game participation, usage-aware redshirt plans/outcomes, one unified roster/recruiting/portal NIL ledger, schema-6 portal-window records, authoritative intent, exact retention, coverage-aware deterministic matching, atomic transfer execution, typed history, whole-root portal integrity, and both scheduled portal windows are checked. | Current college lifecycle, recruiting, redshirt, NIL, portal, and walk-on boundaries are authoritative; extend the same pattern when later tiers add movement rules. | Complete for the M3 college ecosystem; extend with later roster systems. |
| FSC-006 | **Activated in M6.** Cap snapshots, atomic professional acquisitions/releases, schema-10 market phases, deterministic draft classes, observer-specific draft scouting, rookie contracts, guarded promoted-coach market intents, practice-squad movement, trades, waivers, expired-waiver release, sourced contract expiry, and deterministic professional roster AI are live. | The existing `Player.contract`/`ProTeam` model owns transactions; the bounded `ProMarketState` owns dated market identity, waiver deadlines, and phase state. Legacy contracts without a start season remain intentionally non-expiring. | **The both-tier soak now exists (`--pro-soak`) and is red.** It proves cap and roster legality and byte-identical replay, and it proves that in an unattended world the market only opens and closes: `ProMarketSystem.beginDraft`/`draft` are reachable only from a promoted-coach intent, and the free-agent pool starts empty by construction, so no draft pick, signing, waiver or trade ever occurs. A headless driver needs an offseason-calendar rule canon does not state. Cap-vs-rating calibration also remains. |
| FSC-007 | **Activated for college prospects, portal players, and the professional draft in M6.** Observer-specific observations are separate from hidden truth; sealed market snapshots and bounded scouting observations prevent repeated resampling while preserving hidden player truth. Opponent knowledge remains open. | Later tactical/pro milestones must apply the same permitted-knowledge boundary to opponent preparation. | Complete across M4 and M6. |
| FSC-008 | **Partially activated through M2.** Validate minimum roster coverage after every lifecycle transition. **Depth charts exist as of 2026-08-13** (`02` §3.13, `Model/DepthChart.swift`): derived per roster with availability, and able to field a legal eleven a side. Role *assignments* — a package a coach sets, and the persisted overrides against the derivation — remain unavailable, so tactical package eligibility is still open. | M1/M2 own base and rollover coverage; tactics milestone owns the remainder. |
| FSC-012 | **College half activated in M3; professional market intake and roster movement are active in M6.** College departures resolve through signed recruits plus explicit lower-rated walk-ons; no college provisional replacement remains. Professional practice-squad moves, trades, waiver claims, contract expiry, and expiry release use the same copied-root ownership boundary; AI free-agent intake uses the same transaction engine. | Full both-tier durability and roster-quality calibration remain. | Complete in later M6 slices. |
| FSC-009 | **Activated in M3.** Deterministic recruiting AI and delegated controlled-programme recruiting use the same legal action engine as explicit career intents. Persisted responsibility ownership, typed mandatory decisions and resolutions, stable option identities, causal reasons, deadlines, recommendations, actor-owned immutable projections, cancellation-safe commits, and the no-UI-root boundary are authoritative. | Later management systems must extend the same authority and exception pattern rather than bypassing the career session. | Complete for the M3 college ecosystem; extend with later management systems. |
| FSC-010 | **Partially activated in M7.** Rebuildable deterministic history/search projections index current entities, departed identities, rivalries, competition archives, awards, records, and retained typed events. Completed rivalry meetings strengthen the bounded rivalry record, and the same step reinstalls `Programme.rivalIDs` in the order intensity implies. Coaching trees are a rebuildable projection over bounded staff careers. Archived events leave a bounded per-season digest with a ranked sample of bodies. | **Generated news activated 2026-08-12.** `NewsFeedReadModel` renders headlines from typed payloads over the hot journal *and* the archive's retained bodies, ranked by the same `historicalWeight` that decides what an archived season keeps, bounded and never persisted. Cross-season *semantic* narrative - a story that spans seasons rather than reports one event - is still absent, as are programme evolution and conference movement. | Complete in a later M7 slice. |
| FSC-011 | Render matches from recorded movement/decision anchors without resolving football in the UI. | Existing match records explain outcomes but do not yet contain the full top-down animation anchor stream. | Match-presentation milestone after anchor contract. |
| FSC-013 | Validate every recorded participant against the roster they legally belonged to at the game's season and week after in-season movement is enabled. | M3 safely validates against current rosters because ownership is stable while current-season results remain live. Dated roster-tenure/transaction history does not yet exist, so the same check would be untruthful after midseason trades or other in-season moves. | **Trigger is earlier than this row assumed, measured 2026-08-12.** Giving bootstrap professionals contracts makes final-week `expireContracts` release ~315 players while that season's results are still live, and every game they played in immediately fails the participant check. So the activation trigger is **contract expiry at the final week**, not professional trades - and professional roster turnover is blocked on this entry rather than the other way round. |
| FSC-014 | Activate the mechanical effects and deterministic population of `ironman`, `workhorse`, `iceInVeins`, `frontRunner`, `mentor`, `adaptable`, and `volatile`. | **`ironman` and `volatile` activated 2026-08-20; `restless` was already live.** `ironman` now shortens weeks lost through `PeopleRules.injuryWeeks`, which `PeopleLifecycleSystem.processHealth` calls -- the helper existed and had no caller, so the trait was implemented and inert. `volatile` now reaches `DisciplineSystem.incidents`, which had read it since it was written but which no scheduler step called until the weekly `disciplineFile` step; that is the same gate failing one level further out. Both joined `TraitPopulationGenerator.activeTraits` only once their consumer was live, which is the rule this row exists to enforce. `workhorse`, `iceInVeins`, `frontRunner`, `mentor` and `adaptable` remain unpopulated and must stay so until their named systems consume them. | Activate the remaining five with their named authoritative systems. |

## Scheduler activation at the current M3 checkpoint

M3 now additionally executes `scoutingKnowledge`, college `marketInteractions`, and college
`aiDecisions`; each has focused ordering, event, determinism, and integrity tests.

**`disciplineFile` joined the blueprint on 2026-08-20**, between `injuriesAndRecovery` and
`practiceAndDevelopment`. The position is load-bearing rather than cosmetic:
`PeopleLifecycleSystem.processHealth` counts a served suspension down on the `injuriesAndRecovery`
tick, so a suspension drawn before it would be shortened by the very week it was issued in. The step
runs `DisciplineAISystem`, which answers the file at every organisation the player is not coaching
and **skips the controlled one**, because `02` §5.2 makes the response the coach's and a step that
answered on their behalf would be the game administering its own discipline. `DisciplineSystem`
itself is unchanged and stays the mechanism; the policy is the new system beside it, in the same
relationship `ProRosterAISystem` has to `ProMarketSystem`. The detailed user
game, relationships/stakeholders, news/narrative, pro markets, and later systems remain visibly
inactive in their fixed positions. Activation still requires a focused proof of inputs, events,
determinism, and downstream integrity.
